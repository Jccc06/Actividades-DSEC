//10 calcular recorrido y tiempo empleado
let recorrido = [];
let puntoinicial = [];
let punto1 = [];
let resultado;
function succes(pos){
    const lat = pos.coords.latitude;
    const long = pos.coords.longitude;
    recorrido.push([lat,long]);
    if(recorrido.length == 1){
         map = L.map('map').setView([pos.coords.latitude, pos.coords.longitude], 19);

        L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '&copy; <a href="http://www.openstreetmap.org/copyright">OpenStreetMap</a>'
        }).addTo(map);
        punto1.push([lat,long]);
    }
    else{
        resultado = Math.sqrt(Math.pow((recorrido[1] - punto1[1]),2)+Math.pow((recorrido[2] - punto1[2]),2));
        return resultado
    }
   
}

console.log(navigator.geolocation.watchPosition(succes));














































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































